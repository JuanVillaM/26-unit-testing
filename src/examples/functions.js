const regret2 = ( name ) => `Hello ${name}`;
const regret3 = ( name ) => `Hello ${name}`;
const regret4 = () => 'Hello World!';

export const getUser = () => ({
    userId: 'ABCD12321',
    userName: 'THE_CONNOR465'
});

export const getActiveUser = (name) => ({
    userId: 'ABCD414',
    userName: name
});